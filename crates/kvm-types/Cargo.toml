[package]
name = "kvm-types"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "Platform-neutral domain types for Software KVM"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["serde", "v4"] }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
